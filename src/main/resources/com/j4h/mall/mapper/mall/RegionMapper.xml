<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.j4h.mall.mapper.mall.RegionMapper">
  <!--取出省份信息-->
  <select id="getRegionProvinceList" resultType="com.j4h.mall.model.mall.RegionProvince">
    select id,`name`,type,code from cskaoyan_mall_region where pid = 0
  </select>
  <!--取出对应省份的城市-->
  <select id="getRegionCityListByPid" resultType="com.j4h.mall.model.mall.RegionCity">
    select id,`name`,type,code from cskaoyan_mall_region where pid = #{pid}
  </select>
  <!--取出对应城市的区域-->
  <select id="getRegionAreaListByCid" resultType="com.j4h.mall.model.mall.RegionArea">
    select id,`name`,type,code from cskaoyan_mall_region where pid = #{cid}
  </select>
</mapper>
