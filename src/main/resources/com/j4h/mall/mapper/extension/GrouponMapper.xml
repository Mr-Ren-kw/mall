<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.j4h.mall.mapper.extension.GrouponMapper">
    <sql id="queryItems">
        id,order_id as orderId,groupon_id as grouponId,rules_id as rulesId,user_id as userId,creator_user_id as creatorUserId,add_time as addTime,update_time as updateTime,share_urlas as shareUrl,payed,deleted
    </sql>
    <select id="queryGrouponByCondition" resultType="com.j4h.mall.model.extension.groupon.BeanForDatabase.Groupon">
        select <include refid="queryItems"/> from cskaoyan_mall_groupon
        <where>
            deleted = 0
            <if test="goodsId != 0">
                and goods_id = #{goodsId}
            </if>
        </where>
    </select>
</mapper>